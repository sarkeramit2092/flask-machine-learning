# content is just a variable name; it is just the name of that one particula block. content name just one Alias or identifier for this particular block.
# Block is a fixed name.

# Changes reflect dynamically. Like: if anything updates in database (here it is the employees.py file) - dynamically our html code will be generated.




  